================
Custom Iterators
================

.. warning::

  This section has not yet been written. The plan for this part
  of the tutorial is written below.

In the last section we created...

Using `metawards.iterators` to control how the disease is modelled
each day, so that we can model a lockdown.

1. Understanding `metawards.iterators.iterate_default`
2. Changing to a lockdown mode
3. Setting lockdown to start at a specific date
4. Modelling different levels of lockdown
5. Setting lockdown to end at a specific date
6. Setting lockdown to end when a condition is true
7. Triggering differnet levels of lockdown to start and end according
   to set conditions

